# Minimal env for Context Chat Backend (CCBE) with R2R backend
# Copy to ccbe.env (for docker-compose) or to .env when running locally.

# Core app settings
APP_HOST=0.0.0.0
APP_PORT=10034
APP_PERSISTENT_STORAGE=persistent_storage

# Backend selection: keep `builtin` to use upstream behavior, or set `r2r` to use R2R.
# RAG_BACKEND=builtin
RAG_BACKEND=r2r

# URL of the R2R service (use the docker service name if on the same network)
R2R_BASE_URL=http://r2r:7272

# Optional: auth to R2R if enabled
# R2R_API_KEY=your_api_key_here      # sent as X-API-Key
# R2R_API_TOKEN=your_token_here      # sent as Authorization: Bearer <token>

# Optional: request timeout (seconds)
# R2R_HTTP_TIMEOUT=300

